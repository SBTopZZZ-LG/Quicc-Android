<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.sbtopzzz.quicc">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.QuiccApp"
        android:usesCleartextTraffic="true"
        tools:ignore="AllowBackup"
        tools:targetApi="m">
        <activity android:name=".Activities.UserEditEventActivity" />
        <activity android:name=".Activities.UserCreateEventActivity" />
        <activity
            android:name=".Activities.UserSearchActivity"
            android:theme="@style/Theme.MaterialComponents.DayNight.NoActionBarSpecific" />
        <activity android:name=".Activities.UserViewEventActivity" />
        <activity
            android:name=".Activities.UserHomeActivity"
            android:theme="@style/Theme.MaterialComponents.DayNight.NoActionBarSpecific" />
        <activity android:name=".Activities.UserRegisterActivity" />
        <activity android:name=".Activities.UserLoginActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>